apply plugin: 'com.android.instantapp'

android {
    buildToolsVersion rootProject.buildTools
    compileSdkVersion rootProject.compileSdk
}

dependencies {
    implementation project(':movie-base')
    implementation project(':movie_details')
}
